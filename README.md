# Palme Documentation

This directory contains the production Mintlify documentation for the Palme mobile app and backend.

Run Mintlify from this directory after installing the Mintlify CLI:

```bash
cd docs
mintlify dev
```

The sidebar and theme are configured in `mint.json`.
